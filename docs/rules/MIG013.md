# MIG013 — SQL DELETE WITHOUT WHERE

## Problem
DELETE statement without a WHERE clause.

## Why This Is Dangerous
All rows in the table are deleted, usually unintentionally.

## Recommended Solutions
- Add a WHERE clause.
- Use TRUNCATE explicitly if full deletion is intended.
