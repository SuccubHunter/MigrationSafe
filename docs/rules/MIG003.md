# MIG003 — Drop Index Without CONCURRENTLY

## Problem
An index is dropped without using the CONCURRENTLY option.

## Why This Is Dangerous
Dropping an index without CONCURRENTLY can block concurrent queries and writes.

## Recommended Solutions
- Use DROP INDEX CONCURRENTLY.
- Ensure the operation is executed outside of a transaction.
